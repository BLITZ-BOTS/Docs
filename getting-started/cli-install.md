---
description: Install the Blitz CLI to streamline your experience
---

# CLI Install

To install the cli, simply run:

```bash
deno install --global --allow-run -N -R -W -E -n blitz jsr:@blitz-bots/cli
```

After installation, you can use the blitz cli with the prefix "blitz"
